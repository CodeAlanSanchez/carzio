import { PageProps } from "@/types/global";
import Main from "@/Layouts/Main";

export const Home = ({ auth }: PageProps) => {
    return (
        <Main auth={ auth }>

        </Main>
    )
}

export default Home;
